# Build dependencies — used by build_site.yml
# Install: pip install -r requirements.txt
# Versions pinned to what is tested locally (Python 3.12).
# Bump a version here, push, and CI will pick it up.
pandas==2.2.3
pyyaml==6.0.2
pyarrow==18.0.0
yfinance==1.2.0
openpyxl==3.1.5
fredapi>=0.5.1
python-dotenv>=1.0.0
requests>=2.31.0
lxml>=5.0.0
html5lib>=1.1
beautifulsoup4>=4.12.0
pytest==8.3.3
hypothesis==6.152.9
